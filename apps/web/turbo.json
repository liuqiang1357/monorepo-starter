{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**", ".open-next/**", ".vercel/**"]
    },
    "test": {
      "outputs": ["__screenshots__/**"]
    }
  }
}
