{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**", ".open-next/**"]
    },
    "test": {
      "outputs": ["__screenshots__/**"]
    }
  }
}
