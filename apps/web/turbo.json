{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**", ".vercel/**"]
    },
    "test": {
      "outputs": ["__screenshots__/**"]
    }
  }
}
